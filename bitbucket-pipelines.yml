image: atlassian/default-image:3

pipelines:
  default:
    - step:
        name: Build React + Docker Image
        services:
          - docker
        caches:
          - node
        script:
          - npm install
          - npm run build
          - docker build -t frontend-app .

definitions:
  services:
    docker:
      memory: 2048
