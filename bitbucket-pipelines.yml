image: node:22

pipelines:
  default:
    - step:
        name: Install & Build React App
        caches:
          - node
        script:
          - npm install
          - npm run build
        artifacts:
          - dist/**
